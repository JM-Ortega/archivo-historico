# Desired state PROJECT-MANAGED de AtoM: plugins que DEBEN estar habilitados.
#
# Semántica (config/atom/reconcile-plugins.sh, servicio `reconcile`):
#   - un nombre de plugin por línea; `#` inicia un comentario; líneas en blanco ignoradas;
#   - si el plugin falta en la lista de plugins habilitados de AtoM, se AÑADE; si ya está, no se toca nada;
#   - NO es una lista autoritativa: los plugins que no figuran aquí no se gestionan (ni se añaden ni se quitan);
#   - quitar una línea de este fichero NO deshabilita el plugin: este reconcile nunca deshabilita nada.

arUnicaucaB5Plugin
